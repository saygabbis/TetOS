Pasta de documentos locais da TetOS.

Você pode criar arquivos `.txt`, `.md` ou `.json` aqui e acessar pela API:
- GET /documents
- GET /documents/:id
- POST /documents/:id

Também é possível usar /operations para leituras e escritas operacionais controladas.
